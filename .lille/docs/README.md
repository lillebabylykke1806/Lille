# Docs

Long-form project documentation: architecture, ADRs, environment setup, and platform notes (web, iOS, Android).

Add subfolders or markdown files as the project grows. Keep app-specific runtime config in the repo root (`capacitor.config.ts`, `codemagic.yaml`, etc.)—use this folder for **explanations**, not secrets.
